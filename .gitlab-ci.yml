image: node:latest

stages:
  - build
  - test

build:
  before_script:
      - cd backend  
  stage: build
  script:
    - npm install
  artifacts:
    expire_in: 1 days
    when: on_success


test:
    before_script:
        - cd backend      
    stage: test
    coverage: /backend/
    dependencies:
        - build
    script:
        - npm test
    artifacts:
        when: always

